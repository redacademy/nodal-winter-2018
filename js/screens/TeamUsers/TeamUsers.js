import React from "react";
import { Text } from "react-native";
import TeamUserCard from "../../components/TeamUserCard";

const TeamUsers = () => (
  <TeamUserCard onPress={() => {}} fun={3} grow={4} win={3} />
);

export default TeamUsers;
